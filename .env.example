# Database
DB_PASSWORD=changeme_secure_password

# Allowed Email Domains (comma-separated)
ALLOWED_DOMAINS=mhb.de,thesoc.de,mpauli.de

# SMTP (for notifications)
SMTP_SERVER=mail.thesoc.de
SMTP_PORT=587
SMTP_USER=iceosint@thesoc.de
SMTP_PASSWORD=changeme

# AWS (uses EC2 Instance Role if not set)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
AWS_REGION=eu-central-1
